<%= form_for(@importage) do |f| %>
  <% if @importage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@importage.errors.count, "error") %> prohibited this importage from being saved:</h2>

      <ul>
      <% @importage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
